# 2026-09-16 优化方案独立审查

对象：[实施计划](../superpowers/plans/2026-09-16-reviewed-optimizations.md)。
基线为 v0.8.1 / `2ce41c404c6228820bb190b5d41fb3684dfa6e7d`。
本记录只证明方案经审查通过，不代表修复已经完成。

## 证据调查

`audit_runtime_science` 独立复现了直接热图导出的并发覆盖、metadata 单位未进入报告、
NetCDF 默认坐标索引提前整列读取三项问题。
`audit_workbench_adoption` 独立复现了单文件/Zarr 上传登记失败时误删替换目标，
以及长任务被最新 50 条历史遮挡的问题；还核验了修改源文件后的实际 provenance，
据此排除了“Catalog 摘要旧值必然造成错误输出来源”的未经证实结论。

## 技术正确性与可行性

审查者：`review_plan_correctness`，独立于调查者。

首轮结论为 FAIL。阻塞项是：不能将现有清理代码泛称为无条件归属安全；
不能让统一单位解析器的冲突异常导致报告整体生成失败。

计划修订后，审查者明确给出 PASS：

- R1 区分公开目标竞争与对随机私有暂存路径的主动篡改；要求隔离后复核、恢复冲突留证。
- R2 明确报告仍生成、保留 validation.errors、冲突 unit 为 None 且禁止比较受影响数值。
- R3 同时验证有/无任务 context 的读取和索引行为。
- R4 的当前活动任务、重启遗留目录、分页与轮询边界合理。
- R5 的当前入口白名单和历史文档保留规则可实施。

最终无技术阻塞项。

## 范围与验收完整性

审查者：`review_plan_acceptance`，独立于调查者和技术审查者。

首轮 PASS；提出禁止弱化有效测试、记录依赖来源、区分方案通过与实现完成的补强建议。
技术修订和上述建议纳入后，再次复核 PASS：

- 五项均有具体代码入口、真实错误后果及可执行验收。
- 文件/目录清理窗口、单位冲突、真实读取量和活动任务生命周期均有明确断言。
- 保留 Python/API/科学约定、85% 覆盖率及已有 P1/P2 回归。
- 保留用户资产，仅做本地实现；未授权新的外部发布。
- 要求如实记录安装、浏览器和平台验证范围。

最终无范围或验收阻塞项。

## 交接设置

用户已确认沿用当前实际设置，并要求新对话也开启两倍速。
运行记录为 `gpt-6-astra / ultra`；当前桌面版模型目录确认 Fast 服务档 ID 为 `priority`，
描述为 `2x speed, increased usage`。新对话创建与首轮启动均须显式传入并核验这些值。
