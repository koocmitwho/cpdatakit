# v0.8.0 P0 Implementation Plan

**Goal:** Prepare a reviewable, unpublished v0.8.0 candidate and honest dependency/platform evidence.
**Architecture:** Preserve the isolated post-v07 implementation. Pin a feasible lower combination per Python version, validate installed versions, and run the candidate wheel with full tests in each available environment. Retain package dependency declarations unless evidence requires a change.
**Tech Stack:** Python >=3.12, Hatchling, pytest, pip, GitHub Actions.
**Spec:** P0-A/P0-B task handoff; docs/superpowers/plans/2026-09-07-post-v07.md.

## Constraints
- Work only in cpdatakit-next; preserve other checkouts and all existing work.
- Preserve v0.5 API/CLI/schema 1.0/HDF5 1.0 and v0.7 behavior.
- No commit, push, PR, tag or publication is authorized.
- Schema diff/migration, new sources and mesh topology are outside P0.

## Execution
- [x] Inspect tracked/untracked inventory, baseline and canonical remote/main/release/PR state.
- [x] Reproduce lower-bound resolution gaps; add failing runner tests, implement exact requirements and installed-version checks; preserve pip reports and failure evidence.
- [x] Synchronize 0.8.0 metadata and candidate notes without a release date; update project links while preserving scientific snapshots; audit notices and distribution contents.
- [x] Extend each dependency CI cell to build/install the wheel, run full tests and actual Uvicorn/static/legacy/new-workflow smoke.
- [x] Run available Windows and WSL Linux Python 3.12/3.13 lower/latest cells, full coverage >=85%, Ruff and package checks. Record pending macOS/hosted CI explicitly.
- [x] Rebuild wheel/sdist twice, compare bytes and verify metadata/Twine, clean installation and browser regression.
- [x] Deliver file inventory and proposed commit groups, verification cutoff, usage/release commands and draft PR text. Request external-operation authorization only at handoff.

Verification logs go under .artifacts/p0-*; durable summaries go under docs/verification/2026-09-08-v080-p0.md. Existing 2026-09-07 evidence remains historical.

Local work complete; macOS/hosted CI waits for authorization to commit, push and open the candidate PR.
