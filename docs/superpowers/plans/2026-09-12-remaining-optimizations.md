# 剩余优化实施计划

目标：在 P1 提交 `c67f73c` 的基础上，完成 2026-09-11 审阅中剩余的 F07–F11，
以及启动、运行时容量、模块职责、文档和 CI 维护。

当前分支为 `codex/p2-optimizations`。本轮源码优化尚未提交或推送；PyPI 发布另行处理。
按测试先行实施，保留原始数据和其他工作区改动。技术栈保持 Python >=3.12、现有科学计算
依赖、FastAPI、SQLite 和 pytest，不引入新的求解器或物理推断流程。

## 1. 批处理中断恢复与原子发布

- [x] 复现输出已写入但清单未完成的中断，以及写入期间出现同名目标的竞争。
- [x] 提供共享 `publish_file(staged, target, force=False)`，使用原子创建目标操作。
- [x] 提供 Windows、Linux、macOS 对应的独占目录发布路径。
- [x] 执行前保存逐项意图、参数指纹和暂存位置；发布前保存结果与哈希。
- [x] 只恢复具有匹配证据的结果，拒绝已变化的输入、输出或暂存内容。
- [x] 使用进程退出后自动释放的系统锁，并保护配置、映射、组合 schema 和路径别名。
- [x] 验证普通写入、显式覆盖、竞争进程、回滚和清理失败；清理失败不改变已发布结果的成功状态。

## 2. 科学 schema 与报告比较

- [x] 使用真实生成的 schema 2.0 报告复现原比较失败，并验证 100 K 的数值变化。
- [x] 按版本比较维度、坐标、变量、单位和科学约定。
- [x] 读取科学报告的 `statistics.fields`，保留 schema 1.0 的 `numeric_fields` 契约。
- [x] 检查相关坐标的实际单位和形状；缺失统计或不可比数据明确给出原因。
- [x] 验证结构变化、跨版本处理、结果稳定性，以及原有 CLI 和报告接口。

## 3. 启动与 CI 维护

- [x] 测量独立进程的导入、`--version`、`--help`，建立重依赖延迟加载回归测试。
- [x] 延迟导入公共 API 和 CLI 执行服务，保留公共名称、签名及对象身份。
- [x] 对齐 CodeQL init/analyze 引用，并将配套更新归组。
- [x] 将 Ruff 目标对齐 Python 3.12，保留 `JobStatus` 原有字符串行为。
- [x] 保存最终启动基准，与优化前的五次独立进程测量比较。

## 4. 工作台事务、响应与容量

- [x] 复现报告、绘图和比较包在覆盖后登记失败的问题。
- [x] 统一暂存、发布、登记和恢复流程，将产物快照绑定到生成内容的哈希。
- [x] 保留并发产生的外部目标和无法安全恢复的备份，修复任务登记失败时的准入释放。
- [x] 将同步验证、检查、编辑预览、哈希和归档工作移出事件循环，验证健康检查与取消响应。
- [x] 配置待处理任务、完成任务及日志上限；结果持久化后再移除内存记录。
- [x] Catalog v4 保存任务结果，增加项目索引、分页、计数和直接查询；迁移备份包含 WAL 数据。
- [x] 前端按页加载资源摘要，按需读取历史详情，保留分页和任务完成后的选择状态。
- [x] 完成 500 个任务、每任务 1,000 次进度更新的独立进程练习。
- [x] 使用实际 Edge 验证分页、历史详情、热图、下载哈希和移动布局。

## 5. 文档与集成验收

- [x] 更新架构、应用服务、当前工作流和 README 安装入口。
- [x] 核对 GitHub/PyPI 发布差异，将账户侧发布与本轮源码修改分开处理。
- [x] 更新变更日志和本轮验证记录。
- [x] 完成源码全量测试与覆盖率：846 通过、1 跳过、7 条警告，覆盖率 89.07%。
- [x] 完成 Ruff、241 个文件的格式检查，以及 wheel/sdist 构建和发布材料检查。
- [x] 完成独立代码与契约审阅，修复已复现的问题，并验证 Linux 原生发布操作。
- [x] 将最终 wheel 安装至隔离 target，确认导入来源；完成 228 项安装包回归及 CLI/真实 HTTP 验证。

完整结果与验证边界见
[2026-09-12 验证记录](../../verification/2026-09-12-remaining-optimizations.md)。
macOS 原生执行尚未验证。
